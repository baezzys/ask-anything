spring:
  r2dbc:
    url: r2dbc:tc:mysql:///ask_anything?TC_IMAGE_TAG=8.0.30?TC_DAEMON=true?TC_INITSCRIPT=file:src/test/resources/schema.sql
    schema: schema.sql

  config:
    activate:
      on-profile: test